#include<stdio.h>
#include<conio.h>
int fact(int);
int main()
{
    int n,r;
    printf("enter a number");
   scanf("%d",&n);
   r=fact(n);
     printf("the factorial of %d is:%d",n,r);
}
int fact(int n)
{
    if(n>1)
    {
        return n*fact(n-1);
    }
    else
    {
    return 1;
    }
}

